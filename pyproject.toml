[project]
name = "proyecto-final-gestion-datos"
version = "0.1.0"
description = "Proyecto Final de Gestión de Datos: DWH + CLTV + segmentación de clientes (sector salud/bienestar)."
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "Alejandro Barreche" },
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["notebooks", "docs"]

[tool.ruff.lint]
# Explicit rule set so local and CI ruff versions agree.
select = ["E4", "E7", "E9", "F"]
# E402: sys.path manipulation before imports in the dashboard entry points
per-file-ignores = {"dashboard/**" = ["E402"]}